spring:
  application:
    name: eureka-client-software-house
  data:
    mongodb:
      port: 27017
      host: localhost
      database: myCompany
server:
  port: ${PORT:0}

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # Adicione a porta correta do servidor Eureka

# Mostrar Queries MongoDB no Terminal:
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
