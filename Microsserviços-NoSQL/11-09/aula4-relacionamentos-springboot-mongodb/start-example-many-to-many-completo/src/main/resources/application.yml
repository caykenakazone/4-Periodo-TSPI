spring:
  data:
    mongodb:
      port: 27017
      host: localhost
      database: myCompanyManyToMany

# Mostrar Queries MongoDB no Terminal:
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
